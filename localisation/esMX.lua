-- Spanish (MX) locale
local LOCALE = GetLocale()
if LOCALE ~= "esMX" then return end

local L = _G.SecretChecklistLocale or {}

-- Add Spanish (Mexico) translations here

_G.SecretChecklistLocale = L
